# Stash HTTP MITM fixture: parser-owned host policy subset only.
http:
  mitm:
    - stash.example.test
    - "*.stash.example.test"
    - -blocked.stash.example.test
    - weather-data.port-stash.test:*
